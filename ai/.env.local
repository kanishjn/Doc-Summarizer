# Local overrides for development — NOT checked into git
# Example: override Gemini key and use local MySQL if needed
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Local DB (if using Docker mapped port 3307)
DB_HOST=127.0.0.1
DB_PORT=3307
DB_NAME=doc_summarizer
DB_USER=doc_user
DB_PASSWORD=doc_password
